# Credentials
.env
.streamlit/secrets.toml

# All patient data and operational files — never commit
data/*.xlsx
data/*.xls
data/*.csv
output/

# Portable app bundle (USB distribution) — not committed
portable_app/

# WinPython portable distribution — too large to commit
winpython/

# Python
__pycache__/
*.pyc
